import 'package:flutter/material.dart';

class Automations extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Automation"),
        backgroundColor: Colors.blue,
      ),
      body: Center(
        child: Text("Automation Settings Here"),
      ),
    );
  }
}
